import StyledHeader from "./StyledHeader";

const Header = () => {
  return (
    <StyledHeader>
      <h1 className="title">Nedtelling til jul...</h1>
    </StyledHeader>
  );
};

export default Header;
